{% extends 'base.html.twig' %}

{% block title %}Add issue{% endblock %}

{% block body %}

    <div class="mt-4">
        <h1 class="mb-2">Create issue</h1>
        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}

        <button type="submit" class="btn btn-outline-primary" formnovalidate>Send issue</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
